.env
.venv/
__pycache__/
*.pyc
.DS_Store
.claude/

# Terraform: provider binaries are regenerated by `terraform init` (and baked
# into the Docker image separately) — but .terraform.lock.hcl IS tracked, it
# pins provider versions.
infra/terraform/.terraform/
*.tfstate
*.tfstate.*
crash.log
